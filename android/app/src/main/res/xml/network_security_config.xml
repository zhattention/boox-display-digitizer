<?xml version="1.0" encoding="utf-8"?>
<!-- Allow cleartext traffic to local-network IPs. We connect to the Mac
     server via plain ws:// on the LAN (no TLS on LAN). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
